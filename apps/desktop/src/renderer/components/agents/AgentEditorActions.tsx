export function AgentEditorActions({ onCancel, canSave, saving, create = false }: { onCancel: () => void; canSave: boolean; saving: boolean; create?: boolean }) { return <footer className="agent-editor-actions"><button className="agent-button" type="button" disabled={saving} onClick={onCancel}>Cancel</button><button className="agent-button agent-button-primary" type="submit" disabled={!canSave || saving}>{saving ? 'Saving…' : create ? 'Create agent' : 'Save changes'}</button></footer>; }
