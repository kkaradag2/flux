export function MarkdownEditorTabs({ preview, onChange }: { preview: boolean; onChange: (preview: boolean) => void }) { return <div className="markdown-tabs" aria-label="Instructions mode"><button type="button" aria-pressed={!preview} onClick={() => onChange(false)}>Edit</button><button type="button" aria-pressed={preview} onClick={() => onChange(true)}>Preview</button><span>Markdown supported</span></div>; }
