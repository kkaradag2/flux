import type { AgentDefinition } from '../../../shared/management-api';
import { TeamMemberRow } from './TeamMemberRow';
export function TeamMemberList({ ids, agents, organizerAgentId, onChange }: { ids: string[]; agents: AgentDefinition[]; organizerAgentId: string | null; onChange: (ids: string[]) => void }) {
 const move = (index: number, direction: -1 | 1): void => { const next = [...ids]; const target = index + direction; const id = next[index]; const other = next[target]; if (!id || !other) return; next[index] = other; next[target] = id; onChange(next); };
 return <section className="team-members-section"><h2>Current members <small>{ids.length}</small></h2>{ids.length < 2 && <p className="field-help">Add at least two different agents to this team.</p>}<ul className="plain-list">{ids.map((id, index) => { const agent = agents.find(item => item.id === id); return agent ? <TeamMemberRow key={id} agent={agent} organizer={id === organizerAgentId} first={index === 0} last={index === ids.length - 1} onMove={direction => move(index, direction)} onRemove={() => { if (id !== organizerAgentId) onChange(ids.filter(item => item !== id)); }} /> : <li key={id}>Unavailable agent: {id}<button type="button" disabled={id === organizerAgentId} onClick={() => { if (id !== organizerAgentId) onChange(ids.filter(item => item !== id)); }}>Remove</button></li>; })}</ul></section>;
}
