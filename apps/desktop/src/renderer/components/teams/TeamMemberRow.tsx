import type { AgentDefinition } from '../../../shared/management-api';
import { AgentAvatar } from '../avatars/AgentAvatar';
import { AgentStatus } from '../agents/AgentStatus';
import { OrganizerBadge } from './OrganizerBadge';
export function TeamMemberRow({ agent, first, last, organizer = false, onMove, onRemove }: { agent: AgentDefinition; first: boolean; last: boolean; organizer?: boolean; onMove: (direction: -1 | 1) => void; onRemove: () => void }) { return <li className="team-edit-member"><AgentAvatar avatar={agent.avatar} /><span className="member-details"><strong>{agent.name} {organizer && <OrganizerBadge />}</strong><small>Codex · <AgentStatus enabled={agent.enabled} /></small></span><div className="member-actions"><button type="button" className="agent-button" disabled={first} aria-label={'Move ' + agent.name + ' up'} onClick={() => onMove(-1)}>↑</button><button type="button" className="agent-button" disabled={last} aria-label={'Move ' + agent.name + ' down'} onClick={() => onMove(1)}>↓</button><button type="button" className="agent-button" disabled={organizer} title={organizer ? 'Choose another Organizer before removing this member.' : undefined} aria-label={'Remove ' + agent.name} onClick={onRemove}>Remove</button></div></li>; }
