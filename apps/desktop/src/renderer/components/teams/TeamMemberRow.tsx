import type { AgentDefinition } from '../../../shared/management-api';
import { AgentAvatar } from '../avatars/AgentAvatar';
import { AgentStatus } from '../agents/AgentStatus';
export function TeamMemberRow({ agent, first, last, onMove, onRemove }: { agent: AgentDefinition; first: boolean; last: boolean; onMove: (direction: -1 | 1) => void; onRemove: () => void }) { return <li className="team-edit-member"><AgentAvatar avatar={agent.avatar} /><span className="member-details"><strong>{agent.name}</strong><small>Codex · <AgentStatus enabled={agent.enabled} /></small></span><div className="member-actions"><button type="button" className="agent-button" disabled={first} aria-label={'Move ' + agent.name + ' up'} onClick={() => onMove(-1)}>↑</button><button type="button" className="agent-button" disabled={last} aria-label={'Move ' + agent.name + ' down'} onClick={() => onMove(1)}>↓</button><button type="button" className="agent-button" aria-label={'Remove ' + agent.name} onClick={onRemove}>Remove</button></div></li>; }
